import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Smart Bookmark - Your Personal Bookmark Manager",
  description: "A real-time bookmark manager built with Next.js and Supabase. Save, organize, and access your bookmarks from anywhere.",
  keywords: ["bookmark", "manager", "save", "organize", "links"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${inter.variable} font-sans antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
